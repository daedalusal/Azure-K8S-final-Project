apiVersion: v1
kind: Service
metadata:
  name: python-rest-api
  namespace: python-api
  labels:
    app: python-rest-api
spec:
  selector:
    app: python-rest-api
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  type: ClusterIP